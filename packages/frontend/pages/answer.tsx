import AnswerForm from "../components/AnswerForm";
import Layout from "../layouts/default";

const Answer = () => {
  return (
    <Layout>
      <AnswerForm />
    </Layout>
  );
};

export default Answer;
